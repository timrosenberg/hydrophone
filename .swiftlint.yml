excluded:
  - DerivedData
  - .build
  - drafts
  - .worktrees   # per-issue agent worktrees are full source copies (docs/11)

# PlayerModel.swift carries the queue/transport core plus the restore hook;
# separable subsystems already live in extension files (+RemoteCommands,
# +Scrobbling, +PlayQueue). A modest bump beats leaking private state.
file_length:
  warning: 450

identifier_name:
  excluded:
    - id
    - i
    - x
    - y
    - lo
    - hi
